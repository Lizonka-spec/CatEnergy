import { FooterButtons } from "@/shared";

export const Footer = () => {
    return (
        <div className="flex flex-col w-full border-t border-gray-300">
            <FooterButtons />
        </div>
    );
};
